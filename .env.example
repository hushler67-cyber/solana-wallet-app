# Set this in Railway's "Variables" tab. Public mainnet-beta RPC is heavily
# rate-limited — use a private endpoint (Helius, QuickNode, Alchemy, etc.) in production.
SOLANA_RPC_ENDPOINT=https://api.mainnet-beta.solana.com
